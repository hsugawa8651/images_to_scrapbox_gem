RSpec.describe ImagesToScrapboxGem do
  it "has a version number" do
    expect(ImagesToScrapboxGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
